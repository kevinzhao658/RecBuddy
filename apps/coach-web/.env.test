# Loaded by Vite/Vitest in test mode (committed; dummy values only). Unit tests
# mock all queries, so the Supabase client just needs to construct without
# throwing. Real keys live in .env.local (dev) / Vercel (prod).
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
